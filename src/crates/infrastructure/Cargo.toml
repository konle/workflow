[package]
name = "infrastructure"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
futures = "0.3.28"
tokio = {version = "1.48.0", features = ["full"]}
domain = { path = "../domain" }

[dependencies.mongodb]
version = "3.4.1"
